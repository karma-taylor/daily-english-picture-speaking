# 代码与推送风格规范

## Python 代码规范

- 使用 Python 3.12、4 空格缩进、类型标注和 `snake_case` 命名。
- 密钥只能从环境变量读取，不写入源码、日志、示例或工作流文件。
- 所有 HTTP 请求必须指定超时，并置于 `try-except` 中。
- 网络失败需抛出可理解的内部异常，由上层完成降级处理。
- 日志不记录 Token、UID 或完整私密 URL。

## Logging 规范

- 使用标准库 `logging`，默认等级 `INFO`。
- 日志格式：`时间 等级 模块 - 信息`。
- 正常运行记录来源、降级动作和推送成功。
- 失败用 `logger.warning(..., exc_info=True)` 保留诊断堆栈。
- 绝不向微信用户推送异常信息。

## 微信推送极简美学

- 正文只允许一个图片元素。
- 不添加标题、题目、描述、Prompt、来源、链接文字、Emoji 或练习说明。
- 不使用 Markdown 文本消息。
- 图片应主体清晰、具有叙事空间，且适合全年龄用户。
